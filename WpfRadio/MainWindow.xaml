<Window x:Class="WpfRadio.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Radio" Height="120" Width="450"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="UrlBox" Grid.Row="0" Margin="0,0,0,8">
            
        </TextBox>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="PlayBtn" Content="Play" Width="70" Margin="4" Click="Play_Click"/>
            <Button x:Name="StopBtn" Content="Stop" Width="70" Margin="4" IsEnabled="False" Click="Stop_Click"/>
        </StackPanel>
    </Grid>
</Window>